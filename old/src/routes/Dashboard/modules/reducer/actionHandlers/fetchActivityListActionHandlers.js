/**
 * Created by rui on 5/9/17.
 */

// add more handlers for fetching activity list action
let handleFetchingActivityList = (state, payload) => (state);
let handleFetchActivityListFailure = (state, payload) => (state);
let handleFetchActivityListSuccess = (state, payload) => (state);

export {
    handleFetchingActivityList,
    handleFetchActivityListSuccess,
    handleFetchActivityListFailure
}

/**
 * Deleted by rui on 5/23/17.
 */

let handleDeleteActivity = (state, payload) => (state);
let handleDeleteActivityFailure = (state, payload) => (state);
let handleDeleteActivitySuccess = (state, payload) => (state);

export {
    handleDeleteActivity,
    handleDeleteActivitySuccess,
    handleDeleteActivityFailure
}

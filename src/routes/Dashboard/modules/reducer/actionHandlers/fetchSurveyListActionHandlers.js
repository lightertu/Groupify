/**
 * Created by Matt on 5/23/17.
 */

// add more handlers for fetching activity list action
let handleFetchingSurveyList = (state, payload) => (state);
let handleFetchSurveyListFailure = (state, payload) => (state);
let handleFetchSurveyListSuccess = (state, payload) => (state);

export {
    handleFetchingSurveyList,
    handleFetchSurveyListSuccess,
    handleFetchSurveyListFailure
}

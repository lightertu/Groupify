/**
 * Created by rui on 5/23/17.
 */

let handleCreateActivity = (state, payload) => (state);
let handleCreateActivityFailure = (state, payload) => (state);
let handleCreateActivitySuccess = (state, payload) => (state);

export {
    handleCreateActivity,
    handleCreateActivitySuccess,
    handleCreateActivityFailure
}

/**
 * create by Matt  on 5/23/17.
 */

let handleDeleteSurvey = (state, payload) => (state);
let handleDeleteSurveyFailure = (state, payload) => (state);
let handleDeleteSurveySuccess = (state, payload) => (state);

export {
    handleDeleteSurvey,
    handleDeleteSurveySuccess,
    handleDeleteSurveyFailure
}
